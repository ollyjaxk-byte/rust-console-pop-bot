# Contributing

Keep changes read-only, privacy-conscious, and human-readable. Never include real credentials, live IPs, webhook URLs, or private player data. Validate external input, fail clearly on connection errors, and keep secrets out of logs and embeds.

Pull requests should describe the user-visible change, security impact, manual test steps, and environment changes.
